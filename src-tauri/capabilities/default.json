{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capabilities for the main app window, the dictation chip overlay, and the quick-add window",
  "windows": ["main", "overlay", "quickadd"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:default",
    "opener:default",
    "dialog:default"
  ]
}
