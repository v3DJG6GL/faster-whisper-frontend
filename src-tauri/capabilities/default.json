{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capabilities for the main app window, the dictation chip overlay, the quick-add window, and the translation-target picker",
  "windows": [
    "main",
    "overlay",
    "quickadd",
    "langpick"
  ],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:default",
    "core:window:allow-set-size",
    "dialog:default"
  ]
}
